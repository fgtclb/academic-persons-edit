# Aggregate set: the ProfileEditing component under the extension's stable set name.
#
# It carries no TypoScript or page-TSconfig payload of its own. The ProfileEditing
# component owns those files, and listing them here would parse them twice.
#
# Do not rename the set: it is the name this extension has always published, and
# site configurations depend on it.
name: fgtclb/academic-persons-edit
label: Academic Persons Edit
dependencies:
  - fgtclb/academic-persons-edit-profile-editing
